# 🔧 Решение проблем - Twitch Control Mod

## Проблемы при сборке мода

### ❌ Ошибка: "You are using an outdated version of Java"

**Проблема**: Установлена старая версия Java (11 или ниже)

**Решение**:
1. Скачайте и установите Java 17 или выше с https://adoptium.net/
2. Проверьте версию:
   ```bash
   java -version
   ```
3. Если показывает старую версию, установите Java 17 как версию по умолчанию

**Linux/Mac**:
```bash
sudo update-alternatives --config java
```

**Windows**: 
- Установите Java 17
- Добавьте путь к Java 17 в переменную PATH
- Перезапустите терминал

### ❌ Ошибка: "You are using an outdated version of Gradle"

**Проблема**: Gradle версии ниже 8.6

**Решение**:
Откройте `gradle/wrapper/gradle-wrapper.properties` и измените:
```properties
distributionUrl=https\://services.gradle.org/distributions/gradle-8.6-bin.zip
```

### ❌ Ошибка: "Could not resolve all dependencies"

**Проблема**: Gradle не может загрузить зависимости

**Решение**:
1. Проверьте интернет-соединение
2. Очистите кэш Gradle:
   ```bash
   ./gradlew clean --refresh-dependencies
   ```
3. Удалите папку `.gradle` в домашней директории и попробуйте снова

### ❌ Ошибка компиляции в коде

**Проблема**: Синтаксические ошибки или несовместимость API

**Решение**:
1. Убедитесь, что используете правильные версии в `gradle.properties`:
   - minecraft_version=1.20.1
   - loader_version=0.15.11
   - fabric_version=0.92.2+1.20.1
2. Проверьте, что все файлы исходного кода скопированы полностью
3. Попробуйте пересоздать проект с нуля

## Проблемы при запуске мода

### ❌ Мод не появляется в списке модов

**Проблема**: Мод не загружается Fabric Loader

**Решение**:
1. Проверьте, что файл `.jar` находится в папке `.minecraft/mods/`
2. Убедитесь, что установлен Fabric Loader для версии 1.20.1
3. Убедитесь, что установлен Fabric API
4. Проверьте логи: `.minecraft/logs/latest.log`

### ❌ Ошибка: "Incompatible mod set!"

**Проблема**: Конфликт версий или зависимостей

**Решение**:
1. Проверьте версию Minecraft (должна быть 1.20.1)
2. Обновите Fabric API до версии 0.92.2+1.20.1 или выше
3. Удалите другие моды и проверьте, работает ли Twitch Control Mod отдельно
4. Проверьте совместимость с другими модами

### ❌ Игра вылетает при загрузке

**Проблема**: Критическая ошибка в моде или конфликт

**Решение**:
1. Откройте `.minecraft/logs/latest.log` или `crash-reports/`
2. Найдите строки с `twitchcontrol` или `TwitchControlMod`
3. Проверьте, нет ли ошибок `ClassNotFoundException` или `NoSuchMethodError`
4. Если есть, переустановите Fabric API и пересоберите мод

## Проблемы с подключением к Twitch

### ❌ Ошибка: "Failed to connect to Twitch"

**Проблема**: Не удается подключиться к серверам Twitch

**Решение**:
1. Проверьте интернет-соединение
2. Убедитесь, что OAuth токен правильный:
   - Должен начинаться с `oauth:`
   - Получен с https://twitchapps.com/tmi/
   - Не истек (токены не истекают, но могут быть отозваны)
3. Проверьте правильность имени канала:
   - Без символа `#`
   - Только латинские буквы и цифры
   - В нижнем регистре

### ❌ Команда /twitchcontrol не работает

**Проблема**: Команда не распознается

**Решение**:
1. Убедитесь, что мод загружен (проверьте в меню модов)
2. Попробуйте перезайти в мир
3. Проверьте, что вы в одиночной игре или на сервере с разрешенными клиентскими командами
4. Проверьте логи на наличие ошибок при регистрации команд

### ❌ Бот подключается, но не реагирует на команды

**Проблема**: Команды из чата не обрабатываются

**Решение**:
1. Проверьте конфигурацию `.minecraft/config/twitchcontrol.json`:
   - `enableMovement: true`
   - `enableJump: true`
   - и т.д.
2. Проверьте фильтры:
   - Если `subscriberOnly: true`, команды работают только для подписчиков
   - Если `moderatorOnly: true`, команды работают только для модераторов
3. Проверьте кулдаун (`commandCooldown`) - возможно, команды отправляются слишком часто
4. Убедитесь, что команды написаны правильно (регистр не важен)

### ❌ Некоторые команды не работают

**Проблема**: Определенные типы команд не выполняются

**Решение**:
Откройте `.minecraft/config/twitchcontrol.json` и проверьте:
```json
{
  "enableMovement": true,  // для w/a/s/d
  "enableJump": true,      // для jump
  "enableAttack": true,    // для attack
  "enableUse": true        // для use
}
```

Установите нужные параметры в `true` и перезапустите подключение:
```
/twitchcontrol stop
/twitchcontrol start <канал> <токен>
```

## Проблемы с производительностью

### ❌ Игра лагает при активном моде

**Проблема**: Слишком много команд обрабатывается одновременно

**Решение**:
1. Увеличьте кулдаун в конфиге:
   ```json
   "commandCooldown": 2000
   ```
   (2000 = 2 секунды между командами от одного пользователя)

2. Ограничьте доступ:
   ```json
   "subscriberOnly": true
   ```

3. Отключите ненужные команды:
   ```json
   "enableAttack": false,
   "enableInventory": false
   ```

### ❌ Персонаж двигается рывками

**Проблема**: Конфликт между командами или задержка сети

**Решение**:
1. Увеличьте `commandCooldown` до 1500-2000 мс
2. Проверьте стабильность интернет-соединения
3. Уменьшите количество одновременно активных зрителей через фильтры

## Проблемы безопасности

### ⚠️ OAuth токен утек / был показан на стриме

**СРОЧНО**:
1. Немедленно отзовите токен:
   - Зайдите на https://www.twitch.tv/settings/connections
   - Найдите "Twitch Chat OAuth Token Generator"
   - Нажмите "Disconnect"
2. Получите новый токен на https://twitchapps.com/tmi/
3. Обновите конфигурацию с новым токеном
4. Измените пароль Twitch для дополнительной безопасности

### ⚠️ Зрители отправляют вредоносные команды

**Проблема**: Зрители пытаются навредить стримеру в игре

**Решение**:
1. Включите режим только для подписчиков:
   ```json
   "subscriberOnly": true
   ```

2. Или только для модераторов:
   ```json
   "moderatorOnly": true
   ```

3. Отключите опасные команды:
   ```json
   "enableAttack": false
   ```

4. Используйте медленный режим в чате Twitch

## Логи и диагностика

### Где найти логи?

**Логи Minecraft**:
```
.minecraft/logs/latest.log
```

**Crash reports**:
```
.minecraft/crash-reports/
```

### Что искать в логах?

Ищите строки с:
- `[TwitchControl]`
- `twitchcontrol`
- `TwitchControlMod`
- `ERROR` или `WARN`

### Как включить подробное логирование?

Добавьте в `fabric.mod.json`:
```json
"custom": {
  "modmenu": {
    "badges": ["library"],
    "parent": {
      "id": "fabric-api",
      "name": "Fabric API"
    }
  },
  "loom:injected_interfaces": {},
  "fabric-api:module-lifecycle": "stable"
}
```

## Получение помощи

Если проблема не решена:

1. **Соберите информацию**:
   - Версия Minecraft
   - Версия мода
   - Версия Fabric Loader и Fabric API
   - Список других установленных модов
   - Полный текст ошибки из логов

2. **Проверьте известные проблемы**:
   - Прочитайте README.md
   - Проверьте Issues на GitHub (если проект размещен там)

3. **Создайте подробный отчет**:
   - Опишите проблему
   - Укажите шаги для воспроизведения
   - Приложите логи
   - Укажите вашу систему (Windows/Mac/Linux)

## Частые вопросы (FAQ)

**Q: Можно ли использовать мод на сервере?**
A: Мод работает только на клиенте. На сервере он не требуется, но сервер должен разрешать клиентские команды.

**Q: Работает ли мод с другими версиями Minecraft?**
A: Нет, мод создан специально для версии 1.20.1. Для других версий нужна адаптация.

**Q: Можно ли использовать несколько Twitch каналов одновременно?**
A: В текущей версии нет, поддерживается только один канал.

**Q: Безопасно ли хранить OAuth токен в конфиге?**
A: Да, если файл конфигурации не доступен другим пользователям. Не показывайте его на стриме и не публикуйте в открытом доступе.

**Q: Можно ли добавить свои команды?**
A: Да! Откройте исходный код и модифицируйте файлы `TwitchChatBot.java` и `PlayerCommand.java`. Инструкции есть в README.md.

---

**Если ничего не помогло**, опишите проблему максимально подробно и обратитесь за помощью к сообществу Minecraft модостроителей.
