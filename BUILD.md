# 🔨 Инструкция по сборке мода

## Системные требования

- **Java**: версия 17 или выше
- **Gradle**: версия 8.6 или выше (будет загружен автоматически через Gradle Wrapper)
- **Интернет-соединение**: для загрузки зависимостей
- **Свободное место**: ~500 МБ для кэша Gradle и зависимостей

## Подготовка окружения

### Windows

1. **Установка Java 17**:
   - Скачайте с https://adoptium.net/
   - Выберите версию **Temurin 17 (LTS)**
   - Установите с опцией "Add to PATH"
   - Проверьте установку:
     ```cmd
     java -version
     ```
     Должно показать версию 17.x.x

2. **Установка Git** (опционально, для клонирования):
   - Скачайте с https://git-scm.com/download/win
   - Установите с настройками по умолчанию

### macOS

1. **Установка Java 17**:
   ```bash
   brew install openjdk@17
   ```
   
   Или скачайте с https://adoptium.net/

2. **Проверка**:
   ```bash
   java -version
   ```

### Linux (Ubuntu/Debian)

1. **Установка Java 17**:
   ```bash
   sudo apt update
   sudo apt install openjdk-17-jdk
   ```

2. **Проверка**:
   ```bash
   java -version
   ```

## Процесс сборки

### Шаг 1: Получение исходного кода

**Вариант А: Распаковка архива**
```bash
# Для .tar.gz
tar -xzf twitch-control-mod.tar.gz
cd twitch-control-mod

# Для .zip
unzip twitch-control-mod.zip
cd twitch-control-mod
```

**Вариант Б: Клонирование из Git** (если размещено на GitHub)
```bash
git clone https://github.com/your-username/twitch-control-mod.git
cd twitch-control-mod
```

### Шаг 2: Сборка мода

**Windows**:
```cmd
gradlew.bat build
```

**macOS/Linux**:
```bash
chmod +x gradlew
./gradlew build
```

### Шаг 3: Ожидание

Первая сборка займет **5-15 минут**, так как Gradle загрузит:
- Gradle 8.6 (~100 МБ)
- Minecraft 1.20.1 (~50 МБ)
- Fabric Loader и API (~20 МБ)
- Twitch4J и зависимости (~50 МБ)
- Другие библиотеки (~30 МБ)

Вы увидите прогресс:
```
> Task :downloadAssets
> Task :compileJava
> Task :processResources
> Task :classes
> Task :jar
> Task :remapJar
BUILD SUCCESSFUL in 8m 32s
```

### Шаг 4: Получение готового мода

После успешной сборки файл мода будет находиться в:
```
build/libs/twitch-control-mod-1.0.0.jar
```

Также будет создан файл с исходниками:
```
build/libs/twitch-control-mod-1.0.0-sources.jar
```

## Установка собранного мода

1. Скопируйте `build/libs/twitch-control-mod-1.0.0.jar`
2. Вставьте в папку `.minecraft/mods/`
3. Убедитесь, что установлены:
   - Fabric Loader 0.15.11+ для Minecraft 1.20.1
   - Fabric API 0.92.2+1.20.1

## Решение проблем при сборке

### ❌ "java: invalid source release: 17"

**Проблема**: Используется Java версии ниже 17

**Решение**:
1. Установите Java 17
2. Убедитесь, что она используется по умолчанию:
   ```bash
   java -version
   ```
3. Если показывает старую версию:
   - **Windows**: Измените переменную PATH
   - **Linux/Mac**: 
     ```bash
     export JAVA_HOME=/path/to/java-17
     ```

### ❌ "Could not find or load main class org.gradle.wrapper.GradleWrapperMain"

**Проблема**: Gradle Wrapper поврежден

**Решение**:
```bash
# Удалите папку gradle
rm -rf gradle/

# Скачайте заново
wget https://raw.githubusercontent.com/gradle/gradle/master/gradle/wrapper/gradle-wrapper.jar -P gradle/wrapper/
```

### ❌ "Execution failed for task ':compileJava'"

**Проблема**: Ошибки в исходном коде

**Решение**:
1. Проверьте, что все файлы из архива распакованы
2. Убедитесь, что структура проекта правильная:
   ```
   twitch-control-mod/
   ├── src/main/java/com/twitchcontrol/
   ├── src/main/resources/
   ├── build.gradle
   └── gradle.properties
   ```
3. Попробуйте очистить и пересобрать:
   ```bash
   ./gradlew clean build
   ```

### ❌ "Could not resolve all dependencies"

**Проблема**: Нет доступа к репозиториям Maven

**Решение**:
1. Проверьте интернет-соединение
2. Проверьте настройки прокси (если используется)
3. Попробуйте очистить кэш:
   ```bash
   ./gradlew clean --refresh-dependencies
   ```

### ❌ Сборка зависает на "Downloading..."

**Проблема**: Медленное соединение или проблемы с репозиторием

**Решение**:
1. Подождите - первая загрузка может занять до 20 минут
2. Проверьте скорость интернета
3. Попробуйте использовать VPN, если репозитории недоступны

## Пересборка после изменений

Если вы изменили код мода:

```bash
# Быстрая пересборка (без очистки)
./gradlew build

# Полная пересборка (с очисткой)
./gradlew clean build

# Только компиляция (без создания JAR)
./gradlew compileJava
```

## Разработка в IDE

### IntelliJ IDEA (рекомендуется)

1. Откройте IntelliJ IDEA
2. File → Open → выберите папку `twitch-control-mod`
3. Дождитесь импорта Gradle проекта
4. Gradle автоматически настроит все зависимости

### Eclipse

1. Сгенерируйте проект Eclipse:
   ```bash
   ./gradlew eclipse
   ```
2. Откройте Eclipse
3. File → Import → Existing Projects into Workspace
4. Выберите папку `twitch-control-mod`

### Visual Studio Code

1. Установите расширения:
   - Extension Pack for Java
   - Gradle for Java
2. Откройте папку `twitch-control-mod`
3. VSCode автоматически определит Gradle проект

## Запуск тестового клиента

Для тестирования мода без установки в основную игру:

```bash
./gradlew runClient
```

Это запустит Minecraft 1.20.1 с вашим модом уже установленным.

## Создание релиза

Для создания финальной версии мода:

1. Обновите версию в `gradle.properties`:
   ```properties
   mod_version=1.0.1
   ```

2. Соберите мод:
   ```bash
   ./gradlew clean build
   ```

3. Файл готов к публикации:
   ```
   build/libs/twitch-control-mod-1.0.1.jar
   ```

## Публикация мода

### CurseForge

1. Создайте аккаунт на https://www.curseforge.com/
2. Перейдите в раздел Minecraft Mods
3. Нажмите "Create Project"
4. Загрузите JAR файл
5. Заполните описание и скриншоты

### Modrinth

1. Создайте аккаунт на https://modrinth.com/
2. Нажмите "Create a project"
3. Выберите "Mod"
4. Загрузите JAR файл
5. Укажите зависимости (Fabric API)

### GitHub Releases

1. Создайте релиз на GitHub
2. Загрузите JAR файл как asset
3. Добавьте changelog

## Полезные команды Gradle

```bash
# Показать все задачи
./gradlew tasks

# Показать зависимости
./gradlew dependencies

# Проверить код (если настроен checkstyle)
./gradlew check

# Создать только JAR без тестов
./gradlew jar

# Обновить Gradle Wrapper
./gradlew wrapper --gradle-version 8.6
```

## Оптимизация сборки

Для ускорения повторных сборок создайте файл `gradle.properties` в домашней директории:

**Windows**: `C:\Users\YourName\.gradle\gradle.properties`
**Linux/Mac**: `~/.gradle/gradle.properties`

Содержимое:
```properties
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true
org.gradle.jvmargs=-Xmx2048M -Xms512M
```

Это включит:
- Gradle Daemon (фоновый процесс)
- Параллельную сборку
- Кэширование
- Выделение 2 ГБ памяти для Gradle

---

**Успешной сборки! 🛠️**
