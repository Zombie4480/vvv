# 🚀 Быстрый старт - Twitch Control Mod

## За 5 минут до стрима

### 1. Подготовка (делается один раз)

**Установите необходимое:**
- ✅ Minecraft 1.20.1
- ✅ [Fabric Loader](https://fabricmc.net/use/installer/) для версии 1.20.1
- ✅ [Fabric API](https://modrinth.com/mod/fabric-api/version/0.92.2+1.20.1) для версии 1.20.1
- ✅ Java 17 ([скачать](https://adoptium.net/))

**Соберите мод:**
```bash
cd twitch-control-mod
./gradlew build
```

**Установите мод:**
- Скопируйте `build/libs/twitch-control-mod-1.0.0.jar` в папку `.minecraft/mods/`

### 2. Получите OAuth токен

1. Откройте https://twitchapps.com/tmi/
2. Нажмите **Connect** и войдите в свой Twitch аккаунт
3. Скопируйте токен (выглядит как `oauth:abc123def456...`)
4. **ВАЖНО**: Никому не показывайте этот токен!

### 3. Запустите мод

1. Запустите Minecraft с профилем Fabric 1.20.1
2. Зайдите в мир или на сервер
3. Нажмите **T** (открыть чат)
4. Введите команду:
```
/twitchcontrol start ваш_ник oauth:ваш_токен
```

Например:
```
/twitchcontrol start streamer123 oauth:abc123def456
```

### 4. Готово! 🎉

Теперь ваши зрители могут управлять вами, отправляя команды в чат Twitch:

## 📝 Шпаргалка команд для зрителей

### Базовые команды:
| Команда | Действие |
|---------|----------|
| `w` или `вперед` | Идти вперед |
| `s` или `назад` | Идти назад |
| `a` или `влево` | Идти влево |
| `d` или `вправо` | Идти вправо |
| `jump` или `прыжок` | Прыгнуть |
| `sprint` или `бег` | Бежать |

### Дополнительные:
| Команда | Действие |
|---------|----------|
| `attack` или `атака` | Атаковать |
| `use` или `использовать` | Использовать предмет |
| `sneak` или `присесть` | Присесть |

## ⚙️ Быстрые настройки

### Только для подписчиков:
Откройте `.minecraft/config/twitchcontrol.json` и измените:
```json
"subscriberOnly": true
```

### Только для модераторов:
```json
"moderatorOnly": true
```

### Изменить задержку между командами:
```json
"commandCooldown": 2000
```
(значение в миллисекундах, 2000 = 2 секунды)

### Отключить атаки:
```json
"enableAttack": false
```

## 🛑 Остановить мод

В чате Minecraft введите:
```
/twitchcontrol stop
```

## 💡 Советы для стрима

1. **Тестируйте заранее**: Запустите мод за 10-15 минут до стрима
2. **Покажите команды**: Разместите список команд в описании стрима или на экране
3. **Установите кулдаун**: Чтобы избежать хаоса, установите `commandCooldown` на 1-2 секунды
4. **Начните с простого**: Сначала включите только движение, потом добавляйте другие команды
5. **Используйте фильтры**: Для первого стрима можно включить `moderatorOnly` для тестирования

## 🆘 Что-то пошло не так?

### Мод не подключается к Twitch:
- Проверьте правильность OAuth токена
- Убедитесь, что имя канала написано без `#`
- Перезапустите Minecraft

### Команды не работают:
- Проверьте файл конфигурации `.minecraft/config/twitchcontrol.json`
- Убедитесь, что нужные команды включены (`enableMovement: true`)
- Проверьте, не включен ли `subscriberOnly` или `moderatorOnly`

### Игра лагает:
- Уменьшите количество одновременных команд через увеличение `commandCooldown`
- Отключите ненужные типы команд в конфиге

## 📺 Пример для OBS

Добавьте текстовый источник с командами:
```
═══════════════════════════
   УПРАВЛЯЙ СТРИМЕРОМ!
═══════════════════════════
w/вперед  s/назад  a/влево  d/вправо
jump/прыжок  sprint/бег  attack/атака
═══════════════════════════
Кулдаун: 1 секунда
```

---

**Удачного стрима! 🎮🔥**
