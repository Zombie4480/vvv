# Twitch Control Mod для Minecraft 1.20.1

Мод для Minecraft 1.20.1 (Fabric), который позволяет зрителям Twitch управлять игроком через чат. Идеально подходит для стримов формата "Подписчики управляют мной 24 часа".

## 🎮 Возможности

- **Управление движением**: зрители могут отправлять команды для движения персонажа
- **Интеграция с Twitch**: подключение к чату через Twitch4J
- **Гибкая настройка**: возможность включать/выключать различные типы команд
- **Система кулдаунов**: предотвращение спама команд
- **Фильтры доступа**: ограничение команд только для подписчиков или модераторов
- **Поддержка русского и английского языка**: команды работают на обоих языках

## 📋 Требования

- Minecraft 1.20.1
- Fabric Loader 0.15.11 или выше
- Fabric API 0.92.2+1.20.1 или выше
- Java 17 или выше
- Gradle 8.6 или выше (для сборки)

## 🔧 Установка

### Шаг 1: Установка Fabric

1. Скачайте Fabric Installer с [официального сайта](https://fabricmc.net/use/installer/)
2. Запустите установщик и выберите версию Minecraft 1.20.1
3. Нажмите "Install"

### Шаг 2: Установка Fabric API

1. Скачайте Fabric API для версии 1.20.1 с [CurseForge](https://www.curseforge.com/minecraft/mc-mods/fabric-api) или [Modrinth](https://modrinth.com/mod/fabric-api)
2. Поместите файл `.jar` в папку `.minecraft/mods/`

### Шаг 3: Сборка мода

1. Убедитесь, что у вас установлена Java 17:
   ```bash
   java -version
   ```

2. Клонируйте или скачайте исходный код мода

3. Откройте терминал в папке с модом и выполните:
   ```bash
   ./gradlew build
   ```
   (на Windows используйте `gradlew.bat build`)

4. После успешной сборки файл мода будет находиться в `build/libs/twitch-control-mod-1.0.0.jar`

5. Скопируйте этот файл в папку `.minecraft/mods/`

## 🎯 Получение OAuth токена для Twitch

1. Перейдите на [Twitch Chat OAuth Generator](https://twitchapps.com/tmi/)
2. Нажмите "Connect" и авторизуйтесь
3. Скопируйте полученный токен (он будет выглядеть как `oauth:xxxxxxxxxxxxxxxxxxxxxxxxxx`)

**ВАЖНО**: Никогда не делитесь своим OAuth токеном! Это ваш пароль для доступа к чату.

## 🚀 Использование

### Запуск мода в игре

1. Запустите Minecraft с профилем Fabric 1.20.1
2. Зайдите в любой мир или на сервер
3. Откройте чат (клавиша T) и введите команду:
   ```
   /twitchcontrol start <имя_канала> <oauth_токен>
   ```
   Например:
   ```
   /twitchcontrol start mychannel oauth:abcdef123456789
   ```

### Команды для стримера

- `/twitchcontrol start <канал> <токен>` - запустить подключение к Twitch
- `/twitchcontrol stop` - остановить подключение
- `/twitchcontrol status` - проверить статус подключения
- `/twitchcontrol config` - показать текущие настройки

### Команды для зрителей в Twitch чате

#### Движение (русский / английский):
- `w` / `forward` / `вперед` - движение вперед (1 секунда)
- `s` / `back` / `назад` - движение назад (1 секунда)
- `a` / `left` / `влево` - движение влево (1 секунда)
- `d` / `right` / `вправо` - движение вправо (1 секунда)

#### Действия:
- `jump` / `space` / `прыжок` - прыжок
- `attack` / `hit` / `атака` - атака
- `use` / `rightclick` / `использовать` - использование предмета
- `sneak` / `shift` / `присесть` - приседание (1 секунда)
- `sprint` / `run` / `бег` - бег (2 секунды)

## ⚙️ Настройка

Файл конфигурации создается автоматически при первом запуске: `.minecraft/config/twitchcontrol.json`

### Параметры конфигурации:

```json
{
  "twitchChannel": "",
  "oauthToken": "",
  "enableMovement": true,
  "enableJump": true,
  "enableAttack": true,
  "enableUse": true,
  "enableInventory": true,
  "commandCooldown": 1000,
  "subscriberOnly": false,
  "moderatorOnly": false,
  "votingDuration": 10,
  "enableVoting": false
}
```

**Описание параметров:**

- `twitchChannel` - имя вашего Twitch канала
- `oauthToken` - OAuth токен для подключения
- `enableMovement` - разрешить команды движения
- `enableJump` - разрешить прыжки
- `enableAttack` - разрешить атаки
- `enableUse` - разрешить использование предметов
- `commandCooldown` - задержка между командами одного пользователя (в миллисекундах)
- `subscriberOnly` - только подписчики могут отправлять команды
- `moderatorOnly` - только модераторы могут отправлять команды
- `votingDuration` - длительность голосования (в секундах, для будущих версий)
- `enableVoting` - включить режим голосования (для будущих версий)

## 🛠️ Разработка и модификация

### Структура проекта:

```
twitch-control-mod/
├── src/main/java/com/twitchcontrol/
│   ├── TwitchControlMod.java       # Главный класс мода
│   ├── TwitchControlClient.java    # Клиентская часть
│   ├── TwitchChatBot.java          # Интеграция с Twitch
│   ├── PlayerCommand.java          # Обработка команд игрока
│   ├── CommandRegistry.java        # Регистрация игровых команд
│   └── ConfigManager.java          # Управление конфигурацией
├── src/main/resources/
│   ├── fabric.mod.json             # Метаданные мода
│   └── twitchcontrol.mixins.json   # Конфигурация миксинов
├── build.gradle                     # Конфигурация сборки
├── gradle.properties                # Свойства проекта
└── README.md                        # Документация
```

### Добавление новых команд:

Откройте файл `TwitchChatBot.java` и добавьте новые условия в метод `parseCommand()`:

```java
if (message.equals("yourcommand")) {
    return new PlayerCommand(PlayerCommand.Type.YOUR_ACTION, duration);
}
```

Затем добавьте обработку в `PlayerCommand.java` в метод `execute()`.

## 🐛 Решение проблем

### Мод не загружается
- Убедитесь, что установлен Fabric Loader и Fabric API
- Проверьте, что версия Minecraft 1.20.1
- Проверьте логи в `.minecraft/logs/latest.log`

### Не подключается к Twitch
- Проверьте правильность OAuth токена
- Убедитесь, что имя канала указано без символа `#`
- Проверьте интернет-соединение

### Команды не работают
- Проверьте настройки в конфигурационном файле
- Убедитесь, что соответствующие типы команд включены (`enableMovement`, `enableJump` и т.д.)
- Проверьте, не установлен ли фильтр `subscriberOnly` или `moderatorOnly`

### Ошибки при сборке
- Убедитесь, что установлена Java 17 или выше
- Проверьте, что используется Gradle 8.6 или выше
- Очистите кэш Gradle: `./gradlew clean`

## 📝 Лицензия

MIT License - вы можете свободно использовать, модифицировать и распространять этот мод.

## 🤝 Вклад в проект

Если вы хотите улучшить мод:
1. Создайте форк репозитория
2. Внесите изменения
3. Отправьте pull request

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
- Проверьте раздел "Решение проблем" выше
- Изучите логи игры в `.minecraft/logs/latest.log`
- Создайте issue с подробным описанием проблемы

## 🎥 Идеи для стримов

- **Twitch Plays Minecraft** - зрители полностью управляют персонажем
- **Хаос режим** - все зрители могут отправлять команды одновременно
- **Режим голосования** - зрители голосуют за следующее действие (требует доработки)
- **Челлендж на выживание** - попробуйте выжить, когда зрители управляют вами
- **Прохождение с подписчиками** - пройдите игру вместе с чатом

## 🔮 Планы на будущее

- [ ] Режим голосования за действия
- [ ] Поддержка управления инвентарем
- [ ] Интеграция с Channel Points
- [ ] Визуальный оверлей для отображения команд
- [ ] Статистика команд от зрителей
- [ ] Система наград для активных участников
- [ ] Поддержка макросов (цепочки команд)

---

**Приятного стрима! 🎮✨**
